
Connect to a Postgres database:

```bash
psql postgresql://dbUsername:DBUserPassword@hostnameOrIPAddress/databaseName
```


Once connected, if you are seeking for help, you can use the command: `\?`
